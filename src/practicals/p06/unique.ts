const number = string(process.argv[2]);
function getUniqueNumbers(arr1: number[], arr2: number[]): number[] 
const newArray: number[] = arr1.concat(arr2,);
console.log(number.filter ("arr1 == arr2"));
